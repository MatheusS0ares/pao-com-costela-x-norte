# Fotos da landing page

Pasta só pra fotos de vitrine/exibição do site (hero, seções visuais) —
diferente das fotos de cada item do cardápio, que sobem pelo painel
(`/admin/cardapio`) direto pro Supabase Storage, não por aqui.

Exclusiva deste projeto (`pao-com-costela-x-norte/public/`), isolada de
qualquer outro app do monorepo — nada aqui é compartilhado.

Sugestão de nome pra foto de destaque do hero: `hero.jpg`.
